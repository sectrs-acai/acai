diff --git a/Makefile b/Makefile
index 1a1c0c3..33cf017 100644
--- a/Makefile
+++ b/Makefile
@@ -1,4 +1,4 @@
-all: pteditor ptedit.o example header tests
+all: pteditor ptedit.o example header tests libptedit.a libptedit.so
 
 header: module/pteditor.c module/pteditor.h ptedit.c ptedit.h
 	echo "#pragma once" > ptedit_header.h
@@ -25,3 +25,7 @@ clean:
 	cd demos && make clean
 	cd test && make clean
 	rm -f example *.o
+
+libptedit.a: ptedit.o
+	$(AR) rcs $@ $^
+
+libptedit.so: ptedit.o
+	$(CC) $(CFLAGS) -c -fPIC ptedit.c -o libptedit.o
+	$(CC) $(CFLAGS) -shared libptedit.o -o libptedit.so